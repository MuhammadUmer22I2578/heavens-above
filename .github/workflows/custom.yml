name: Custom Workflow Integration

on:
  push:
    tags:
      - 'v*'  # Triggers only when a tag like v1.0.0 or v2.1 is pushed

jobs:
  generate-release-notes:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate GitHub release notes
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref_name }}
          release_name: "Release ${{ github.ref_name }}"
          draft: false
          prerelease: false
          body: |
            🚀 **New Release:** ${{ github.ref_name }}
            Automatically generated by GitHub Actions.
            See commit history for details:
            https://github.com/${{ github.repository }}/commits/${{ github.ref_name }}

      # (Optional) Upload artifacts or documentation
      # - name: Upload release assets
      #   uses: actions/upload-release-asset@v1
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   with:
      #     upload_url: ${{ steps.create_release.outputs.upload_url }}
      #     asset_path: ./dist/my-app.zip
      #     asset_name: my-app.zip
      #     asset_content_type: application/zip
